# koydo-anvil-releases environment template
# Copy to .env.local or the deployment provider's environment settings.
# Keep real secrets out of Git.

# Add app-specific non-secret placeholders here as integrations are enabled.
